import { FilterManifiestoPipe } from './filter-manifiesto.pipe';

describe('FilterManifiestoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterManifiestoPipe();
    expect(pipe).toBeTruthy();
  });
});
