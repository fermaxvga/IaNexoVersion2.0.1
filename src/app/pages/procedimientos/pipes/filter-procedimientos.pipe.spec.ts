import { FilterProcedimientosPipe } from './filter-procedimientos.pipe';

describe('FilterProcedimientosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProcedimientosPipe();
    expect(pipe).toBeTruthy();
  });
});
