import { FilterPostTagPipe } from './filter-post-tag.pipe';

describe('FilterPostTagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPostTagPipe();
    expect(pipe).toBeTruthy();
  });
});
